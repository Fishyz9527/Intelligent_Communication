<Window x:Class="C_Sharp_file.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:C_Sharp_file"
        mc:Ignorable="d"
        Title="Intelligent_Communication" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--#region 输入区域 -->
        <TextBox x:Name="txtInput"
         Grid.Row="0"
         AcceptsReturn="True"
         VerticalScrollBarVisibility="Auto"
         Height="100"
         Margin="0,0,0,10"/>
        <!--#endregion-->

        <!--#region 操作按钮 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
            <Button Content="语法检查"
            Click="BtnCheck_Click"
            Width="100" Height="50" Margin="5"/>
            <Button Content="翻译"
            Click="BtnTranslate_Click"
            Width="100" Height="50" Margin="5"/>
            <Button Content="润色"
            Click="BtnPolish_Click"
            Width="100" Height="50" Margin="5"/>
        </StackPanel>
        <!--#endregion-->

        <!--#region 结果显示 -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <ListView x:Name="lstIssues"
              Grid.Column="0"
              Margin="0,0,5,0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="位置"
                                DisplayMemberBinding="{Binding start}"
                                Width="100"/>
                        <GridViewColumn Header="建议"
                                DisplayMemberBinding="{Binding message}"
                                Width="100"/>
                        <GridViewColumn Header="替换" 
                                DisplayMemberBinding="{Binding replacements}" 
                                Width="100"/>
                    </GridView>
                </ListView.View>
            </ListView>
            <TextBox x:Name="txtOutput"
             Grid.Column="1"
             IsReadOnly="True"
             VerticalScrollBarVisibility="Auto"
             Margin="5,0,0,0"
             TextWrapping="Wrap"
             AcceptsReturn="True"/>
        </Grid>
        <!--#endregion-->
    </Grid>
</Window>
